// src/api/base44client.jsx

export const base44Client = {
  get: async (url) => ({ data: {} }),
  post: async (url, data) => ({ data: {} }),
  put: async (url, data) => ({ data: {} }),
  delete: async (url) => ({ data: {} })
};

export const base44 = base44Client;
export default base44Client;